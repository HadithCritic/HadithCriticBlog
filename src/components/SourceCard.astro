---
interface Props {
  work: string;
  author: string;
  death?: string;
}

const { work, author, death } = Astro.props;
---

<div class="source-card">
  <h4>{work}</h4>
  <p><strong>Author:</strong> {author}</p>
  {death && <p><strong>Died:</strong> {death}</p>}
</div>
