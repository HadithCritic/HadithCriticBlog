<figure class="isnad-dilemma" aria-labelledby="isnad-dilemma-title">
  <figcaption class="isnad-dilemma__heading">
    <span class="isnad-dilemma__kicker">Evidence method</span>
    <h2 id="isnad-dilemma-title">The isnad dilemma</h2>
    <p>Reported numbers and recoverable sources answer different questions.</p>
  </figcaption>

  <div class="isnad-dilemma__cases">
    <section class="isnad-dilemma__case" aria-labelledby="dilemma-courtroom-title">
      <div class="isnad-dilemma__case-heading">
        <span class="isnad-dilemma__index">01</span>
        <div>
          <span class="isnad-dilemma__label">Analogy</span>
          <h3 id="dilemma-courtroom-title">Courtroom scenario</h3>
        </div>
      </div>
      <blockquote class="isnad-dilemma__quote">
        <p>“Your Honor, five hundred people saw the defendant commit the crime.”</p>
        <cite>Illustrative testimony</cite>
      </blockquote>
      <dl class="isnad-dilemma__measures">
        <div><dt>People reported</dt><dd>500</dd></div>
        <div><dt>Account described</dt><dd>1</dd></div>
      </dl>
    </section>

    <section class="isnad-dilemma__case" aria-labelledby="dilemma-history-title">
      <div class="isnad-dilemma__case-heading">
        <span class="isnad-dilemma__index">02</span>
        <div>
          <span class="isnad-dilemma__label">Historical report</span>
          <h3 id="dilemma-history-title">The five hundred witnesses</h3>
        </div>
      </div>
      <blockquote class="isnad-dilemma__quote">
        <p>“Christ appeared to more than five hundred brothers at once.”</p>
        <cite>1 Corinthians 15:6</cite>
      </blockquote>
      <dl class="isnad-dilemma__measures">
        <div><dt>People reported</dt><dd>500</dd></div>
        <div><dt>Written account cited</dt><dd>1</dd></div>
      </dl>
    </section>
  </div>

  <p class="isnad-dilemma__note">
    A reported group size does not, by itself, establish how many independent accounts preserve the claim.
  </p>
</figure>

<style>
  .isnad-dilemma {
    --dilemma-accent: var(--hc-gold, #c6a15b);
    margin: clamp(2rem, 4vw, 3rem) 0;
    padding: 1.15rem 0 0;
    border-top: 1px solid var(--hc-rule, rgba(210, 200, 180, 0.24));
    color: var(--hc-text-primary, #eee9df);
  }

  .isnad-dilemma__heading {
    margin-bottom: 1.15rem;
  }

  .isnad-dilemma__kicker,
  .isnad-dilemma__label {
    color: var(--hc-text-tertiary, #aaa398);
    font-family: var(--font-ui, sans-serif);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .isnad-dilemma__heading h2 {
    margin: 0.3rem 0 0.15rem;
    font-family: var(--font-display, serif);
    font-size: clamp(1.35rem, 2vw, 1.65rem);
    font-weight: 500;
    line-height: 1.2;
    color: var(--hc-text-primary, #eee9df);
  }

  .isnad-dilemma__heading p {
    margin: 0;
    color: var(--hc-text-secondary, #c9c1b5);
    font-size: 0.94rem;
    line-height: 1.5;
  }

  .isnad-dilemma__cases {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    border-block: 1px solid var(--hc-rule-soft, rgba(210, 200, 180, 0.14));
  }

  .isnad-dilemma__case {
    min-width: 0;
    padding: 1rem 1.15rem 0.9rem 0;
  }

  .isnad-dilemma__case + .isnad-dilemma__case {
    padding-inline: 1.15rem 0;
    border-inline-start: 1px solid var(--hc-rule-soft, rgba(210, 200, 180, 0.14));
  }

  .isnad-dilemma__case-heading {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .isnad-dilemma__index {
    color: var(--dilemma-accent);
    font-family: var(--font-display, serif);
    font-size: 0.93rem;
    font-variant-numeric: tabular-nums;
  }

  .isnad-dilemma__case-heading h3 {
    margin: 0.12rem 0 0;
    color: var(--hc-text-primary, #eee9df);
    font-family: var(--font-display, serif);
    font-size: 1.08rem;
    font-weight: 500;
    line-height: 1.3;
  }

  .isnad-dilemma__quote {
    margin: 0 0 0.8rem;
    padding-inline-start: 0.8rem;
    border-inline-start: 2px solid color-mix(in srgb, var(--dilemma-accent) 62%, transparent);
  }

  .isnad-dilemma__quote p {
    margin: 0;
    color: var(--hc-text-secondary, #c9c1b5);
    font-family: var(--font-body, sans-serif);
    font-size: 0.96rem;
    font-style: italic;
    line-height: 1.5;
  }

  .isnad-dilemma__quote cite {
    display: block;
    margin-top: 0.3rem;
    color: var(--hc-text-tertiary, #aaa398);
    font-size: 0.78rem;
    font-style: normal;
  }

  .isnad-dilemma__measures {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin: 0;
    border-top: 1px solid var(--hc-rule-soft, rgba(210, 200, 180, 0.14));
  }

  .isnad-dilemma__measures div {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.6rem;
    padding-top: 0.55rem;
  }

  .isnad-dilemma__measures dt {
    color: var(--hc-text-tertiary, #aaa398);
    font-size: 0.76rem;
    line-height: 1.35;
  }

  .isnad-dilemma__measures dd {
    margin: 0;
    color: var(--hc-text-primary, #eee9df);
    font-family: var(--font-display, serif);
    font-size: 1.05rem;
    font-variant-numeric: tabular-nums;
  }

  .isnad-dilemma__note {
    max-width: 70ch;
    margin: 0.75rem 0 0;
    color: var(--hc-text-tertiary, #aaa398);
    font-size: 0.82rem;
    line-height: 1.5;
  }

  :global([data-theme="light"]) .isnad-dilemma {
    --dilemma-accent: #8b6b32;
  }

  @media (max-width: 640px) {
    .isnad-dilemma__cases {
      grid-template-columns: 1fr;
    }

    .isnad-dilemma__case,
    .isnad-dilemma__case + .isnad-dilemma__case {
      padding: 0.85rem 0;
      border-inline-start: 0;
    }

    .isnad-dilemma__case + .isnad-dilemma__case {
      border-top: 1px solid var(--hc-rule-soft, rgba(210, 200, 180, 0.14));
    }
  }
</style>
