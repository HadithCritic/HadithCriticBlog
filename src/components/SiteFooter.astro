---
---
<footer class="site-footer">
  <div class="footer-top-border"></div>
  <div class="wrap footer-grid">
    <div class="footer-brand">
      <a href="/" class="footer-brand-logo">
        <img src="/geometric_emblem_transparent.png" alt="" class="footer-emblem" />
        <span class="footer-brand-name hc-title">HadithCritic</span>
      </a>
      <p class="footer-desc hc-copy">
        HadithCritic is dedicated to the study of reports through evidence, transmission, source history, and textual comparison.
      </p>
    </div>

    <div class="footer-navs">
      <div class="footer-col">
        <strong class="hc-label">Directory</strong>
        <nav>
          <a href="/blogs" class="hc-header-link">Blog</a>
          <a href="/academia" class="hc-header-link">Academia</a>
          <a href="/projects" class="hc-header-link">Projects</a>
          <a href="/resources" class="hc-header-link">Resources</a>
        </nav>
      </div>
      <div class="footer-col">
        <strong class="hc-label">Media</strong>
        <nav>
          <a href="/youtube" class="hc-header-link">YouTube</a>
          <a href="/method" class="hc-header-link">Method</a>
          <a href="/contact" class="hc-header-link">Contact</a>
        </nav>
      </div>
    </div>
  </div>
  
  <div class="wrap footer-base">
    <p class="hc-small">&copy; {new Date().getFullYear()} HadithCritic. Verification over reputation.</p>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;
    margin-top: auto;
    padding-top: 4rem;
    background: var(--hc-black-soft);
  }

  .footer-top-border {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: url("/assets/textures/parchment-card-light.png") repeat-x;
    border-top: 1px solid var(--hc-gold-dim);
    border-bottom: 1px solid var(--hc-rule);
    opacity: 0.8;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1.5fr 2fr;
    gap: 4rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid var(--hc-rule-soft);
  }

  .footer-brand-logo {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    text-decoration: none;
    margin-bottom: 1rem;
  }

  .footer-emblem {
    height: 32px;
    width: auto;
    object-fit: contain;
    filter: brightness(0) saturate(100%) invert(80%) sepia(35%) saturate(546%) hue-rotate(345deg) brightness(88%) contrast(92%);
  }

  .footer-brand-name {
    color: var(--hc-gold);
    font-size: 1.8rem;
  }

  .footer-desc {
    max-width: 40ch;
  }

  .footer-navs {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .footer-col nav {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    margin-top: 1.2rem;
  }

  .footer-base {
    padding: 2rem 0;
    text-align: center;
  }

  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
</style>
