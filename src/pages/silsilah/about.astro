---
import SilsilahLayout from '../../layouts/SilsilahLayout.astro';
import { loadIndex, translationLabel, fmtCount } from '../../lib/silsilah';

const { collections } = loadIndex();

const rows = collections.map((c) => {
  const tr = translationLabel(c);
  const aiPct = c.englishCount ? Math.round((c.aiCount / c.englishCount) * 100) : 0;
  return { ...c, tr, aiPct };
});
---

<SilsilahLayout title="Sources and Coverage | Silsilah">
  <section class="hc-section">
    <div class="hc-container about-wrap">
      <span class="hc-eyebrow">Sources and coverage</span>
      <h1 class="about-title">What Silsilah contains, and how honestly it is labelled</h1>
      <div class="about-prose">
        <p>
          Silsilah carries {fmtCount(rows.reduce((s, c) => s + c.hadithCount, 0))} reports across
          {rows.length} classical collections. Every report keeps its Arabic text, its chain of
          transmission, the compiler's arrangement of books and chapters, and the gradings recorded
          against it. Where an English translation exists it is shown alongside the Arabic.
        </p>
        <p>
          Translation provenance matters. Reports whose English text is AI-assisted are marked
          individually with an <strong>AI translation</strong> tag, and machine-translated chapter
          introductions carry an <strong>MT</strong> tag. Two collections, Ṣaḥīḥ Ibn Khuzaymah and
          Musnad al-Bazzār, currently have no English translation; they are browsable in Arabic and
          are labelled Arabic only.
        </p>
        <p>
          Gradings are reported, not endorsed. A grade chip reflects the judgement of the named
          grader (Albānī, Arnaʾūṭ, Zubayr ʿAlī Zaʾī, the compiler himself, and others as recorded).
          HadithCritic's own position is that verification runs through evidence, not reputation;
          the gradings are part of the historical record being examined.
        </p>
      </div>

      <div class="about-table-wrap">
        <table class="about-table">
          <thead>
            <tr>
              <th>Collection</th>
              <th>Reports</th>
              <th>Books</th>
              <th>English coverage</th>
              <th>AI-assisted share</th>
            </tr>
          </thead>
          <tbody>
            {rows.map((c) => (
              <tr>
                <td><a href={`/silsilah/${c.slug}/`}>{c.nameEn}</a></td>
                <td>{fmtCount(c.hadithCount)}</td>
                <td>{c.bookCount}</td>
                <td>
                  <span class={`sil-badge sil-badge--${c.tr.tone}`}>
                    {c.englishPct < 5 ? 'None' : c.englishPct + '%'}
                  </span>
                </td>
                <td>{c.englishPct < 5 ? '·' : c.aiPct + '% of translated reports'}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div class="about-prose">
        <p>
          Numbering follows the reference scheme of the underlying editions, so citations such as
          <code>bukhari:2487</code> or <code>malik:1-3</code> resolve directly through
          <a href="/silsilah/find/">the reference lookup</a>. Corrections and source reports are
          welcome through the <a href="/contact">HadithCritic contact page</a>.
        </p>
      </div>
    </div>
  </section>
</SilsilahLayout>

<style>
  .about-wrap {
    max-width: 900px;
  }

  .about-title {
    margin: 0.8rem 0 0;
    color: var(--hc-parchment);
    font-family: var(--font-display);
    font-weight: 500;
    font-size: clamp(2rem, 4vw, 2.9rem);
    line-height: 1.05;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }

  .about-prose {
    margin-top: 1.4rem;
    color: var(--hc-muted);
    line-height: 1.72;
  }

  .about-prose p {
    margin: 0 0 1.1rem;
    max-width: 72ch;
  }

  .about-prose strong {
    color: var(--hc-parchment);
  }

  .about-prose a {
    color: var(--hc-gold);
  }

  .about-prose code {
    padding: 0.08rem 0.4rem;
    border: 1px solid var(--hc-rule);
    border-radius: var(--radius-xs);
    background: rgba(216, 177, 102, 0.07);
    color: var(--hc-gold-soft);
    font-size: 0.85em;
  }

  .about-table-wrap {
    margin: 1.6rem 0;
    overflow-x: auto;
    border: 1px solid var(--hc-rule);
  }

  .about-table {
    width: 100%;
    border-collapse: collapse;
    font-family: var(--font-ui);
    font-size: 0.85rem;
  }

  .about-table th {
    text-align: left;
    padding: 0.7rem 0.9rem;
    color: var(--hc-gold);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--hc-rule);
    background: rgba(216, 177, 102, 0.05);
    white-space: nowrap;
  }

  .about-table td {
    padding: 0.6rem 0.9rem;
    border-bottom: 1px solid var(--hc-rule-soft);
    color: var(--hc-muted);
    white-space: nowrap;
  }

  .about-table td a {
    color: var(--hc-parchment);
    text-decoration: none;
  }

  .about-table td a:hover {
    color: var(--hc-gold);
  }

  .sil-badge {
    padding: 0.16rem 0.5rem;
    border-radius: var(--radius-pill);
    border: 1px solid var(--hc-rule);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.05em;
  }

  .sil-badge--full { color: #9fc492; border-color: rgba(159, 196, 146, 0.35); background: rgba(127, 165, 114, 0.08); }
  .sil-badge--partial { color: var(--hc-gold-soft); background: rgba(216, 177, 102, 0.08); }
  .sil-badge--none { color: var(--hc-ash); background: rgba(140, 138, 134, 0.08); }
</style>
